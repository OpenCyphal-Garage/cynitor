// Placeholder. When the dashboard is served by cynitor-server, the backend
// replaces this file with a generated one declaring its own origin, so the
// page talks to the server it came from rather than to localhost:8080.
//
// Served by a plain static file server during development, this empty version
// is what loads, and the address field keeps its built-in default.
